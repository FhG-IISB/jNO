name: Python package

on:
  push:
    branches: [ "v2" ]
  pull_request:
    branches: [ "v2" ]

jobs:
  build:

    strategy:
      fail-fast: false
      matrix:
        # Windows is excluded: JAX has no official Windows support.
        # ubuntu-22.04 removed: ubuntu-latest (24.04) is sufficient.
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4

    - name: Install system dependencies (Ubuntu)

      if: startsWith(matrix.os, 'ubuntu')
      run: |
        sudo apt-get update
        sudo apt-get install -y libglu1-mesa

    - name: Install system dependencies (macOS)

      if: matrix.os == 'macos-latest'
      run: |
        brew install mesa-glu

    - name: Install uv

      uses: astral-sh/setup-uv@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies

      # cuda extra is meaningless in CI (no GPU); skip it on all platforms.
      run: uv sync --all-extras --no-extra cuda

    - name: Test with pytest

      run: uv run pytest