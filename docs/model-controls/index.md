# Model Controls

!!! info "Moved"
    The model-control API (optimizer, freeze, mask, LoRA, dtype, diagnostics, tune) is now
    documented under **[Operations → Part B](../operations.md#part-b-operations-that-require-trainable-parameters)** —
    the operations that require trainable parameters.
